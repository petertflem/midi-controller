#include "Library.h"
#include "Bank.h"

/*Library::Library()
{
  for (byte i = 0; i < NUMBER_OF_BANKS; i++)
  {
    _banks[i] = new Bank("Just a name");
  }

  _currentBankIndex = 0;
}

/* ========== PUBLIC FUNCTIONS ========== */
/*void Library::incrementBank()
{
  if (_currentBankIndex == NUMBER_OF_BANKS - 1)
  {
    _currentBankIndex = 0;
  } else {
    _currentBankIndex++;
  }
}

void Library::decrementBank()
{
  if (_currentBankIndex == 0)
  {
    _currentBankIndex = NUMBER_OF_BANKS - 1;
  } else {
    _currentBankIndex--;
  }
}

Bank* getSpecificBank(String bankName)
{
  for (byte i = 0; i < NUMBER_OF_BANKS; i++)
  {
    if (_bank[i]->getBankName() == bankName)
      return _bank[i];
  }

  return nullptr;
}

byte Library::getCurrentBankIndex()
{
  return _currentBankIndex;
}

Bank* Library::getCurrentBank()
{
  return _banks[_currentBankIndex];
}*/
